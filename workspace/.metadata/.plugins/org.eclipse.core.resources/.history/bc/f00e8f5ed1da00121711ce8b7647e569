package br.com.acad.dao.generico;

import java.util.List;

public interface DAO<T,K> {
	
	void insert(T entity);
	
	void removeById(K id);
	
	T searchByID(K id);
	
	void update(T entity);
	
	List<T> searchAll();
}